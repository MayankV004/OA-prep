global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'oa-prep-app'
    metrics_path: '/api/metrics'
    params:
      format: ['prometheus']
    static_configs:
      - targets: ['host.docker.internal:3000']
