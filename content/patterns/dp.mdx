export const metadata = {
  title: 'Dynamic Programming',
  description: 'Break problems into overlapping subproblems, solve each once, and store results to avoid redundant computation.',
  timeComplexity: 'O(n²) or O(n×m) typical; problem-specific',
  spaceComplexity: 'O(n) or O(n×m); often reducible',
  useCases: ['Fibonacci / climbing stairs', 'Knapsack variants', 'Longest common subsequence', 'Coin change', 'Edit distance'],
  variations: [
    { id: '1d', title: '1-D DP (Linear)', description: 'State depends only on a single index. Classic: Fibonacci, House Robber, Climbing Stairs.' },
    { id: '2d', title: '2-D DP (Grid / Two Sequences)', description: 'State depends on two indices (i, j). Classic: LCS, Edit Distance, Unique Paths.' },
    { id: 'knapsack', title: 'Knapsack / Subset Sum', description: '0/1 or unbounded — can you pick items to hit a target? dp[i][w] = max value with i items and capacity w.' },
    { id: 'interval', title: 'Interval DP', description: 'State is a range [i, j]. Build answers for larger intervals from smaller ones. Classic: Matrix Chain, Burst Balloons.' },
    { id: 'bitmask', title: 'Bitmask DP', description: 'State encodes a subset as a bitmask. Classic: Travelling Salesman, Shortest Path visiting all nodes.' },
  ],
};

## Concept

DP works when a problem has:
1. **Optimal substructure** — optimal solution is built from optimal sub-solutions
2. **Overlapping subproblems** — same subproblem is solved multiple times in recursion

The two implementation styles:
- **Top-down (Memoization)** — recursion + cache
- **Bottom-up (Tabulation)** — iterative, fill a table

> **Key insight**: Define the state `dp[i]` (or `dp[i][j]`) precisely: *"the answer to the subproblem with parameter i (and j)"*. Then write the recurrence.

## Template

### 1-D DP — House Robber

```python
def rob(nums: list[int]) -> int:
    if len(nums) == 1:
        return nums[0]

    dp = [0] * len(nums)
    dp[0] = nums[0]
    dp[1] = max(nums[0], nums[1])

    for i in range(2, len(nums)):
        dp[i] = max(dp[i - 1], dp[i - 2] + nums[i])

    return dp[-1]

# Space-optimized:
def rob_optimized(nums: list[int]) -> int:
    prev2, prev1 = 0, 0
    for n in nums:
        prev2, prev1 = prev1, max(prev1, prev2 + n)
    return prev1
```

### 2-D DP — Longest Common Subsequence

```python
def lcs(text1: str, text2: str) -> int:
    m, n = len(text1), len(text2)
    dp = [[0] * (n + 1) for _ in range(m + 1)]

    for i in range(1, m + 1):
        for j in range(1, n + 1):
            if text1[i - 1] == text2[j - 1]:
                dp[i][j] = dp[i - 1][j - 1] + 1
            else:
                dp[i][j] = max(dp[i - 1][j], dp[i][j - 1])

    return dp[m][n]
```

### 0/1 Knapsack

```python
def knapsack(weights: list[int], values: list[int], capacity: int) -> int:
    n = len(weights)
    dp = [[0] * (capacity + 1) for _ in range(n + 1)]

    for i in range(1, n + 1):
        for w in range(capacity + 1):
            # Don't take item i-1
            dp[i][w] = dp[i - 1][w]
            # Take item i-1 if it fits
            if weights[i - 1] <= w:
                dp[i][w] = max(dp[i][w], dp[i - 1][w - weights[i - 1]] + values[i - 1])

    return dp[n][capacity]
```

### Top-Down Memoization Template

```python
from functools import lru_cache

def solve(n: int) -> int:
    @lru_cache(maxsize=None)
    def dp(i: int, j: int) -> int:
        # base case
        if i == 0 or j == 0:
            return 0
        # recurrence
        return dp(i - 1, j - 1) + 1   # example — replace with actual logic

    return dp(n, n)
```

## When to Use

- Counting ways to reach a goal
- Finding the min/max cost to reach a target
- Any problem asking "can we achieve X?" with overlapping sub-decisions
- String comparison / alignment problems

## Complexity Analysis

| Type | Time | Space |
|------|------|-------|
| 1-D DP | O(n) | O(n) → O(1) optimized |
| 2-D DP | O(n×m) | O(n×m) → O(n) optimized |
| Knapsack | O(n×W) | O(n×W) → O(W) optimized |

## Common Pitfalls

- Defining the **state** incorrectly or too broadly
- Not correctly initializing base cases (off-by-one in DP table)
- Forgetting that 0/1 knapsack iterates weights in **reverse** when space-optimizing
- Confusing memoization (top-down) and tabulation (bottom-up) index ordering
